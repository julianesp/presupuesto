{
  "name": "sistema-presupuestal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names BACKEND,FRONTEND --prefix-colors cyan,magenta --kill-others-on-fail \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend-cloudflare && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:backend": "cd backend-cloudflare && npm run deploy",
    "install:all": "npm install && cd backend-cloudflare && npm install && cd ../frontend && npm install",
    "db:migrate": "cd backend-cloudflare && npm run db:migrate",
    "add-user": "cd backend-cloudflare && npm run add-user"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
